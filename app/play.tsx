import React from "react";
import { StyleSheet, Text, View } from "react-native";

const play = () => {
	return (
		<View>
			<Text>play</Text>
		</View>
	);
};

export default play;

const styles = StyleSheet.create({});
