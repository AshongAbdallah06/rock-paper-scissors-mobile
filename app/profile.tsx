import React from "react";
import { Text, View } from "react-native";

const profile = () => {
	return (
		<View className="bg-red-500">
			<Text className="text-3xl justify-center">profile</Text>
		</View>
	);
};

export default profile;
